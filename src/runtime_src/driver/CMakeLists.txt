add_subdirectory(include)
if (${CMAKE_SYSTEM_PROCESSOR} STREQUAL x86_64)
  add_subdirectory(xclng)
  add_subdirectory(common_em)
  add_subdirectory(cpu_em)
  add_subdirectory(hw_em)
else()
  add_subdirectory(zynq)
endif()
